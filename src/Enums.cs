namespace Celeste.Mod.RainTools {
    public enum ColorRGBAAlphaChangeMode {
        ColorTimesPrevA,
        ColorTimesAlpha,
        ColorOnly,
        AlphaOnly,
        ColorAndAlpha
    }

    public enum ColorAlphaChangeMode {
        ColorOnly,
        AlphaOnly,
        Both
    }

    public enum ColorgradeAlphaChangeMode {
        ColorgradeOnly,
        AlphaOnly,
        Both
    }

    public enum BlurLayerChangeMode {
        OnlyBlur1,
        OnlyBlur2,
        Both
    }
}